<?php

namespace App\InsertionSort;

class SecondAttemptAtIterationSort
{
    public function sort(array $elements)
    {
        $totalElements = count($elements);
//        $sortedElements = $elements[0];
//
//        for ($index = 1; $index < $totalElements; $index++) {
//
//            while ($elements[$index] > $elements[$index - 1]) {
//
//            }
//        }
        return $elements;
    }
}
